# Foundational requirements

The other `.md` files in this directory are **foundational requirements**: they set out the database and data model, workflow behaviour, and user-story structure. They allow us to bootstrap the project (schema, seed data, and design references) before building applications on top.

These are design references, not machine-parsed specs. They guide tooling and evolution.