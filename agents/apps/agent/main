#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
VENV_PY="$SCRIPT_DIR/../../.venv/bin/python"

cd "$SCRIPT_DIR/work" || exit 1
exec "$VENV_PY" "$SCRIPT_DIR/main.py" "$@"

